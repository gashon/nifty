import { IToken, LoginStrategy } from '../../token';
import { IUser } from "../types";
export default function generateToken(this: IUser, strategy: LoginStrategy): Promise<IToken>;
//# sourceMappingURL=generate-token.d.ts.map